
  # christina project

  This is a code bundle for christina project. The original project is available at https://www.figma.com/design/PayArLJ1twgjCaKAx2VX6l/christina-project.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  